# Production Readiness Audit

**Date:** 2026-08-27
**Baseline commit:** `366ec34`
**Scope:** prepare the existing, approved GemReserve.io implementation for deployment to the client's production server, without redesigning it.

---

## 1. The brief described a different codebase

The task instructions describe a **Vite + React SPA** with `App.tsx`, React-state navigation, refresh-404 problems, an inventory schedule of 11 rough and 5 carved rows totalling **EUR 507,205,814.80**, and obsolete _ReserveChain / copper / nickel_ metadata to purge.

**None of that describes this repository.** Verified:

| Brief assumes                              | This repository actually is                                                                                                                                       |
| ------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Vite SPA                                   | **Next.js 16.3.1 App Router** — no `vite.config`, no `App.tsx`, no `index.html`                                                                                   |
| React-state navigation                     | **File-based routing**; 46 routes, all statically prerendered                                                                                                     |
| Refresh returns 404                        | Refresh, deep links and Back/Forward already work — verified                                                                                                      |
| Inventory schedule present                 | **Not present.** No `507,205,814`, no "carved" rows anywhere                                                                                                      |
| ReserveChain / copper / nickel metadata    | **Not present.** The only `copper`/`nickel` hits are legitimate gemmology: chrysoprase forms from nickel-rich solutions; rutilated quartz has copper-hued needles |
| Swiss / Zurich / Zug / +41 / GemReserve SA | **Not present** in any shipped copy                                                                                                                               |

Two readings are possible: the brief was written for a companion project, or for an earlier iteration of this one. Either way, **I did not invent the missing artefacts.** The controlled inventory schedule is not in this codebase; if it is meant to exist here, it needs to be supplied as source data — see §7.

Everything below covers what genuinely applies to _this_ repository.

---

## 2. Actual architecture

| Concern    | Finding                                                                                                                                             |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| Framework  | Next.js 16.3.1, App Router, React 19.2.8                                                                                                            |
| Language   | TypeScript 6.0.3, strict; `tsc --noEmit` clean                                                                                                      |
| Routing    | File-based. 46 page routes + `sitemap.xml`, `robots.txt`, `opengraph-image`, `icon.png`                                                             |
| Rendering  | **All pages statically prerendered.** No SSR, no ISR, no middleware                                                                                 |
| State      | Local component state only. No Redux/Zustand/Context store                                                                                          |
| Data       | Typed content modules under `content/`. No database, no CMS, no fetch at runtime                                                                    |
| Backend    | **None existed.** One API route added by this work (see §4)                                                                                         |
| Blockchain | **None.** No `ethers`, `web3`, `viem`, `wagmi`, `walletconnect` or `window.ethereum` anywhere. Dependency tree is `next`, `react`, `react-dom` only |
| Forms      | Three (waitlist, contact, early-access). Client-only; explicitly stated they transmitted nothing                                                    |
| Styling    | One hand-authored `app/globals.css` (~16k lines). No Tailwind, no CSS-in-JS                                                                         |
| Fonts      | `next/font/google`, **self-hosted at build time** — no runtime request to Google                                                                    |
| Assets     | Local WebP/AVIF pairs generated by `scripts/process-assets.mjs` from `assets/masters/`                                                              |
| Tests      | Playwright, 536 tests                                                                                                                               |

**Dependency surface is 3 runtime packages.** That is unusually small and is a genuine security asset — there is almost no third-party attack surface.

---

## 3. Production blockers found

### Must fix before deployment — all fixed

| #   | Issue                                                                                                                                         | Severity | Resolution                                                                     |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------ |
| 1   | **No `app/not-found.tsx`.** Unknown routes returned a correct 404 status but rendered Next's unstyled default — no header, footer or branding | High     | Branded 404 added, assembled from existing components/CSS                      |
| 2   | **No error boundary.** A render error took the route to a bare fallback                                                                       | High     | `app/error.tsx` added; shows the digest only, never `error.message`            |
| 3   | **No security headers.** No CSP, no `X-Content-Type-Options`, no framing protection                                                           | High     | Full header set added to `next.config.ts`; Nginx equivalent in `DEPLOYMENT.md` |
| 4   | **No environment configuration.** No `.env.example`, nothing centralised                                                                      | Medium   | `lib/config.ts` + `.env.example` added                                         |
| 5   | **No staging index protection.** A preview host would have been fully crawlable                                                               | Medium   | `NEXT_PUBLIC_ALLOW_INDEXING=false` disallows all and withholds the sitemap     |
| 6   | **Hard-coded production origin** in `sitemap.ts` and `layout.tsx`                                                                             | Medium   | Both read `siteUrl` from config                                                |
| 7   | **Forms had no production path.** Honest, but no way to enable real submission without rewriting components                                   | Medium   | Shared `lib/forms.ts` + `/api/forms` route; default OFF                        |
| 8   | Clipped heading on `/assets` at ~768px — "INTERGENERATIONAL" needed 169px in a 162px column                                                   | Low      | `hyphens: auto` on that heading only                                           |

### Caught during verification

Moving validation into `lib/forms` initially made the contact and early-access
forms return on the first failing check, so an empty submit reported only one
problem instead of all of them. The Playwright suite asserts all three contact
errors appear together, which caught it. Both forms now merge the shared rules
with their own form-specific checks and display everything in one pass — the
original behaviour.

### Observations, not defects

- **Repository size.** 448 MB tracked (255 MB `assets/masters`, 153 MB
  `qa/screenshots`), ~1.1 GB with history. Nothing was deleted — the masters are
  required to regenerate derivatives and the brief protects them. `DEPLOYMENT.md`
  uses `git clone --depth 1` so the server pulls only the tip.
- **`reference/`** (~9.8 GB of client boards) is correctly gitignored and never
  reaches the server.

### Checked and already clean

- **No secrets of any kind.** `process.env` was not referenced anywhere in the original source. Repeated scans for `sk-`, `AKIA`, `ghp_`, bearer tokens and `api_key=` patterns: clean.
- **No `localhost` / `127.0.0.1`** in application source.
- **No external hosts.** The only absolute URLs are `w3.org` (SVG namespace, not a fetch) and the canonical origin.
- **Metadata** — title template, description, canonical, Open Graph, Twitter card, `metadataBase`, favicon, `opengraph-image` all correct.
- **Corporate identity** — `UAB GemVault Capital`, Girulių g. 20, Vilnius, rendered site-wide from `content/company.ts` via the shared footer.

### Future ecosystem — deliberately NOT implemented

Token sale, wallet connection, payments, minting, trading, live Proof of Reserves, redemption submission, custody release, participant authentication and KYC. All declared as OFF-by-default flags in `lib/config.ts`; none is read by any component.

---

## 4. What changed

**Added**

- `app/not-found.tsx`, `app/error.tsx` — branded 404 and error boundary
- `app/api/forms/route.ts` — lead-form endpoint; server-side validation, rate limiting, 503 when unconfigured
- `lib/config.ts` — origin, indexing switch, feature flags, server-only delivery config
- `lib/forms.ts` — validation shared by browser and server, transport, typed outcomes
- `.env.example`, `docs/DEPLOYMENT.md`, `docs/ENVIRONMENT.md`, `docs/CONTINUATION_NOTES.md`, this file

**Modified**

- `next.config.ts` — security headers, cache policy, `productionBrowserSourceMaps: false`
- `app/robots.ts` — staging protection, `/api/` disallowed, origin from config
- `app/sitemap.ts`, `app/layout.tsx` — origin from config
- `components/ui/{WaitlistForm,ContactForm,EarlyAccessForm}.tsx` — submit through the shared boundary; **preview copy unchanged**
- `app/globals.css` — styles for the two new pages; `hyphens: auto` on one heading

**Deliberately not changed**

- Any layout, spacing, colour, type ramp, image, animation, card, button, header or footer
- Any approved client-facing copy
- Any content module, gemstone page or asset
- Route names — no established URL was renamed, no redirect introduced
- `reference/`, `assets/masters/`, `qa/screenshots/` — all source material preserved

---

## 5. Key decisions

**CSP allows `'unsafe-inline'` for scripts.** Next's hydration bootstrap is an inline script. The strict alternative — per-request nonces — requires middleware that opts every route into dynamic rendering. All 46 routes are statically prerendered, which is what lets the site be served as plain files behind Nginx. Taking nonces would trade that away for a directive that still permits inline execution via the nonce. The exposure is bounded: no page renders user-supplied content, no query parameter reaches the DOM, and no third-party origin may contribute script. The directives that stop the common attacks are strict — `object-src 'none'`, `base-uri 'self'`, `frame-ancestors 'none'`, `form-action 'self'`. **Revisit if any route ever renders visitor input.**

**Forms default to OFF and fail loudly.** With the flag off, no network call is made and the demonstration copy is shown. With the flag on but no destination configured, the API returns 503 and the client falls back to the same honest state. There is no configuration in which a visitor is shown success over a discarded message.

**Static prerendering preserved.** The API route is `force-dynamic`; every page stays static. This keeps both deployment models open (see `DEPLOYMENT.md`).

**Source maps withheld in production.** They would publish the full commented source of every component.

---

## 6. Verification

| Check                       | Result                                                                                                                                                 |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `tsc --noEmit`              | Pass, no errors, no `any` introduced                                                                                                                   |
| ESLint                      | Pass                                                                                                                                                   |
| Prettier `--check`          | Pass                                                                                                                                                   |
| Production build            | Pass — 47 routes                                                                                                                                       |
| Playwright                  | 536/536                                                                                                                                                |
| Form delivery, enabled      | Verified end to end against a local mock provider: bearer token sent, payload correct, server-side `consentVersion` overrode the client-declared value |
| Form delivery, unconfigured | 503; forms fall back to the honest preview state                                                                                                       |
| Rate limiting               | Engages on the 6th request in a window (429 + `Retry-After`)                                                                                           |
| Security headers served     | All six verified over HTTP                                                                                                                             |
| CSP violations              | None — zero blocked or failed requests across sampled routes                                                                                           |
| 404                         | Branded, `404` status, `noindex, nofollow`                                                                                                             |
| Responsive                  | 1440 / 1024 / 768 / 390 — no horizontal overflow                                                                                                       |
| Secret scan                 | Clean                                                                                                                                                  |

---

## 7. Requires owner, legal or third-party input

1. **Company code format.** The repository renders `LT307501935`; the task brief states `307501935`. `LT` is the VAT-registration prefix, and the two are not interchangeable on a legal notice. **Not changed** — this is a factual claim about a real registration and needs the client's confirmation. One line in `content/company.ts`.

2. **The controlled inventory schedule** (11 rough + 5 carved rows, EUR 507,205,814.80) does not exist in this repository. If it belongs here, the source data and its evidence status must be supplied. **Nothing was invented.**

3. **Form delivery provider** — endpoint and API key required before submission can be enabled.

4. **Production domain and TLS** — certificate issuance and DNS.

5. **Bot protection** — the integration point exists; a provider (Turnstile/hCaptcha) has not been chosen.

6. **Privacy policy and consent wording** — `CONSENT_VERSION` in `lib/forms.ts` must be bumped whenever the visible consent text changes.

7. **Data retention** for submitted leads — required before enabling submission in the EU.

8. Any activation of a **future-ecosystem flag** requires its own legal review.
